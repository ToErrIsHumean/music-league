1. For filereads after the initial read, read by anchor: use rg -n to find target lines, then open only the needed range. Do not read whole files unless the task requires holistic understanding.
2. Load context once: after loading the task entry and its referenced contracts/preserves/validates, do not reopen those sections unless the active concern shifts.
