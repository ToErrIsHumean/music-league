### Reviewer Verdict — TASK-08

**AC Audit** (`validates:` from §6)

| AC | Criterion (§5 text) | Status | Evidence |
|----|---------------------|--------|----------|
| AC-01 | `/`, `/games/[id]`, `/games/[id]/rounds/[id]`, `/songs`, `/songs/[songId]`, and `/players/[id]` render as stable, shareable routes with meaningful titles and status-notice degradation for invalid IDs. | `satisfied` | Round loader returns `ready` props for valid route IDs and status notices for invalid game, invalid round, missing game, missing round, and round-outside-game cases at `src/archive/route-skeletons.js:1062`; shell route data carries round title/description at `src/archive/route-skeletons.js:1265`. Route skeleton test covers valid routes and invalid round contexts at `prisma/tests/route-skeletons.test.js:392`; `npm test -- prisma/tests/route-skeletons.test.js` passed. |
| AC-11 | Game round-list rows link to real round routes and render a secondary outbound `Playlist` link only when `Round.playlistUrl` is present; playlist links use `target="_blank"` and `rel="noopener"`. | `satisfied` | Round content renders the playlist badge only when `playlistUrl` exists and sets `href`, `target="_blank"`, and `rel="noopener"` at `src/archive/route-skeletons.js:2090`; route test asserts real round route hrefs at `prisma/tests/route-skeletons.test.js:530`. |
| AC-12 | Round pages render parent game context, optional playlist pill, up to three highlights, and full submissions ordered by rank with comments inline. | `satisfied` | Parent game link, H1, description/date, optional highlights, and submissions list render at `src/archive/route-skeletons.js:2082`; highlights are capped in `buildRoundHighlights` at `src/archive/route-skeletons.js:556`; submissions sort by rank, submitted date, then ID at `src/archive/route-skeletons.js:559`; comments render inline at `src/archive/round-vote-disclosures.js:231`. |
| AC-13 | Round vote evidence is co-located beneath each submission behind initially collapsed `button[aria-expanded]` controls, with per-row Show/Hide labels, non-interactive no-vote rows, and an Expand all votes affordance. | `satisfied` | Disclosure state defaults to an empty `Set` at `src/archive/round-vote-disclosures.js:21`; per-submission controls use `button`, `aria-expanded`, `aria-controls`, and Show/Hide labels at `src/archive/round-vote-disclosures.js:134`; no-vote rows render non-interactive text at `src/archive/round-vote-disclosures.js:130`; Expand all toggles expandable rows at `src/archive/round-vote-disclosures.js:170`. Static route test asserts initial collapsed markup, Show labels, no-vote copy, and no `<details>` at `prisma/tests/route-skeletons.test.js:534`. |
| AC-18 | Every leaderboard row, submission submitter, and vote voter links to `/players/[id]`; every submission song links to `/songs/[songId]`. | `satisfied` | Round page props assign submission song hrefs via `buildSongHref`, submitter hrefs via `buildPlayerHref`, and vote voter hrefs via `buildPlayerHref` at `src/archive/route-skeletons.js:615`; route test asserts those hrefs for submissions and votes at `prisma/tests/route-skeletons.test.js:433`. |
| AC-21 | No page introduces album, genre, mood, release-year, audio-feature, recommendation, authentication, live voting, live submission, playlist management, or Spotify enrichment behavior. | `satisfied` | Diff adds only read-only round rendering, vote disclosure state, route data, CSS, and tests; playlist usage remains an outbound `href` at `src/archive/route-skeletons.js:2090` and no added code references prohibited metadata/enrichment surfaces in the diff. |
| AC-22 | Each primary route satisfies the M8 accessibility checklist: one H1, labeled landmarks, `aria-current`, table captions/headers, focus indicators, skip link, search label/live region, keyboard-operable disclosures, and an arrow-key navigable game switcher with `Enter`/`Space` selection and `Escape` close behavior. | `satisfied` | Round route renders a single H1 at `src/archive/route-skeletons.js:2087`, labeled sections at `src/archive/route-skeletons.js:2102` and `src/archive/round-vote-disclosures.js:163`, and keyboard-operable disclosure buttons at `src/archive/round-vote-disclosures.js:138`. Route skeleton test asserts skip link, search role, switcher control, table caption, and collapsed disclosure markup at `prisma/tests/route-skeletons.test.js:522`; `npm test -- prisma/tests/route-skeletons.test.js` passed. |
| AC-23 | An archive containing at least one current game and one completed game is screenshot-coherent across all primary routes at desktop and mobile widths. | `unverifiable` | The diff contains CSS for round-specific layout at `app/globals.css:1422` and static markup coverage, but no screenshot artifact or browser-render evidence for desktop and mobile widths across all primary routes. |
| AC-24 | Round vote disclosures and player voting-history tables attribute votes only through same-round song matches; recurring songs in other rounds or games never receive those votes. | `satisfied` | Round loader groups votes with `mapVotesToRoundSubmissions` at `src/archive/route-skeletons.js:1211`; the helper keys attribution by `roundId` plus `songId` and rejects missing/duplicate matches at `src/archive/m8-derivations.js:411`. Existing derivation test covers same-round song attribution, and the targeted unit command passed. |

**Invariant Audit** (`preserves:` + repo constitutional)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| INV-01 | spec | `preserved` | Round navigation remains a real `/games/[gameId]/rounds/[roundId]` route via route href assertions at `prisma/tests/route-skeletons.test.js:530`; no added code parses or renders `?round=`, `?song=`, `?player=`, or `?playerSubmission=` overlays. |
| INV-02 | spec | `preserved` | Round vote disclosure uses inline buttons and panels at `src/archive/round-vote-disclosures.js:126`; route test asserts no `role="dialog"`, no `archive-overlay`, and no `<details>` at `prisma/tests/route-skeletons.test.js:549`. |
| INV-06 | spec | `preserved` | Round page uses shared derivation helpers for familiarity and vote attribution at `src/archive/route-skeletons.js:626` and `src/archive/route-skeletons.js:1211`; badge models route through `buildArchiveBadgeModel` at `src/archive/round-vote-disclosures.js:78`. |
| INV-08 | spec | `preserved` | New round CSS resolves palette styling through named tokens such as `--brand-purple`, `--surface-paper`, `--surface-secondary`, and `--ink-muted-rgb` at `app/globals.css:1422`; no new literal palette values are introduced. |
| INV-09 | spec | `preserved` | `Round.playlistUrl` renders only as an outbound badge link with `target="_blank"` and `rel="noopener"` at `src/archive/route-skeletons.js:2090`; no Spotify API, embed, or artwork behavior is added. |
| INV-11 | spec | `preserved` | Round loader keeps rounds scoped to games, submissions as player-song-in-round rows, votes as score evidence, and comments as display evidence at `src/archive/route-skeletons.js:1096`; comments are rendered separately from scores at `src/archive/round-vote-disclosures.js:231`. |
| INV-18 | spec | `preserved` | Vote attribution uses `roundId + songId` in `mapVotesToRoundSubmissions` at `src/archive/m8-derivations.js:416`; round UI consumes `votesBySubmissionId` at `src/archive/route-skeletons.js:628`, not song-only buckets. |
| INV-20 | spec | `preserved` | Round badges use stable variants (`rank-tie`, `rank-plain`, `score`, familiarity variants, and `playlist-link`) at `src/archive/round-vote-disclosures.js:214` and `src/archive/route-skeletons.js:2091`; labels are passed as copy, not style keys. |
| AGENTS canonical guidance | guidance | `preserved` | The diff does not touch `AGENTS.md` or replace it with another guidance source. |
| bolder-utils/default SDD tooling | guidance | `preserved` | The diff does not modify SDD package-bin wiring or tooling configuration. |
| repo-local prompt override location | guidance | `preserved` | The diff does not add repo-local prompt overrides outside `docs/sdd/`. |
| `docs/sdd/` runtime artifact home | guidance | `preserved` | Runtime review/diff artifacts remain under `docs/sdd/`; the implementation diff does not relocate them. |
| Orchestrator-only PLAN writes | guidance | `preserved` | The diff contains no `PLAN-*.md` edits. |
| no implicit active-spec mutation | guidance | `preserved` | The diff contains no spec contract or acceptance-criteria edits. |
| dependency discipline | guidance | `preserved` | The diff adds no package dependency changes. |

**Contract Audit** (`contracts:` → §4 items)

| Contract ref | §4 item | Status | Evidence |
|--------------|---------|--------|----------|
| §4a-3 | Round route | `fulfilled` | `getRoundPageData` distinguishes invalid game ID, invalid round ID, missing game, missing round, and round-outside-game statuses at `src/archive/route-skeletons.js:1062`; ready route data includes parent game context, playlist URL, highlights, submissions, and vote disclosures at `src/archive/route-skeletons.js:586`. |
| §4c-4 | Round page and vote disclosure | `fulfilled` | Props include the specified round, highlights, submission, song, submitter, familiarity, comment, and vote row fields at `src/archive/route-skeletons.js:595`; UI renders `button[aria-expanded]`, non-interactive no-vote text, and Expand all at `src/archive/round-vote-disclosures.js:130` and `src/archive/round-vote-disclosures.js:170`. |
| §4d-5 | Song familiarity verdict | `fulfilled` | Round submissions use `deriveArchiveSongFamiliarity(submission.songId, songAppearances)` at `src/archive/route-skeletons.js:626`; the helper derives archive-wide exact-song first-time/returning verdicts by song ID at `src/archive/m8-derivations.js:272`. |
| §4d-6 | Page data loaders | `fulfilled` | `getRoundPageData(gameId, roundId, input)` accepts Prisma injection, returns `readyRouteData`/`notFoundRouteData`, and owns route invalid states at `src/archive/route-skeletons.js:1062`. |
| §4d-12 | Round vote disclosure state | `fulfilled` | `useRoundVoteDisclosureState` exposes `isExpanded`, `toggleSubmission`, `toggleAll`, and `allExpanded`; it defaults collapsed and filters to expandable IDs at `src/archive/round-vote-disclosures.js:15`. |
| §4d-16 | Vote-to-submission attribution | `fulfilled` | Round loader calls `mapVotesToRoundSubmissions` with submission `roundId` and `songId` at `src/archive/route-skeletons.js:1211`; the helper resolves through the same-round key and throws on duplicates or missing targets at `src/archive/m8-derivations.js:416`. |
| §4d-18 | Badge and registry display models | `fulfilled` | Local `ArchiveBadge` builds the registered badge model and emits stable `data-archive-badge-variant`/role attributes at `src/archive/round-vote-disclosures.js:78`; round UI uses registered variants for rank, score, familiarity, and playlist pills. |
| §4d-19 | Archive visual tokens | `fulfilled` | Added round CSS consumes named M8 tokens and token RGB aliases for colors at `app/globals.css:1422`, `app/globals.css:1450`, and `app/globals.css:1479`; no new route-specific literal palette is introduced. |

**Verdict:** `deferred`

AC-23 is unverifiable from the diff alone because no desktop/mobile screenshot artifact or browser-render evidence across all primary routes was provided.
