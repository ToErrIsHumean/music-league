***

### Reviewer Verdict — TASK-08

**AC Audit** (`validates:` from §6)

| AC | Criterion (§5 text) | Status | Evidence |
|----|---------------------|--------|----------|
| AC-05 | Attempting to commit any batch that is not `ready` fails and leaves canonical tables unchanged | `satisfied` | `src/import/commit-batch.js:17-21,118-133,142-173` enforces the `ready`/no-blocking-issues preconditions before transactional writes; `src/import/commit-batch.test.js:838-924` verifies both parsed and manually-blocked batches reject without canonical mutations. |
| AC-06 | Committing a `ready` batch creates or upserts the required canonical players, rounds, artists, songs, submissions, and votes with correct relationships, hydrated source-derived fields, and `sourceImportId` set on committed submissions and votes | `satisfied` | `src/import/commit-batch.js:23-116,226-595` builds deterministic payloads, upserts canonical identities, writes submissions/votes with `sourceImportId`, and returns affected round ids; `src/import/commit-batch.test.js:405-637` verifies hydrated player/artist/song/round fields, correct relationships, recomputed results, and `sourceImportId` on committed submissions and votes. |
| AC-07 | A batch containing a vote that cannot be attached to a submission in the same round fails validation and cannot commit | `satisfied` | Per the instruction, existing code was inspected for this AC: `src/import/analyze-batch.js:680-700` marks such votes as blocking `unresolved_ref` issues, `src/import/analyze-batch.js:73-77,141-156` turns the batch `failed`, `src/import/commit-batch.js:17-21,142-173` refuses non-`ready` or blocking-issue batches, and `src/import/analyze-batch.test.js:316-377` verifies the failure/repair cycle. |
| AC-08 | Re-ingesting and committing the same logical bundle, or a later full-snapshot bundle with the same derived `gameKey`, does not create duplicate canonical players, rounds, artists, songs, submissions, or votes and removes stale game-scoped rounds, submissions, and votes omitted from the incoming bundle | `satisfied` | `src/import/commit-batch.js:304-707` uses replay-safe upsert keys plus stale vote/submission/round deletion scoped to `batch.gameKey`; `src/import/commit-batch.test.js:645-833` verifies a second full-snapshot commit reuses globals, avoids duplicates, and removes omitted game-scoped rows. |
| AC-09 | After commit, every affected round has `Submission.score` and `Submission.rank` values equal to those derived from canonical `Vote` rows, with unvoted submissions left null | `satisfied` | `src/import/commit-batch.js:82-85` recomputes results inside commit, and `src/import/recompute-round-results.js:35-129` derives score/rank from canonical votes only, applying dense ranks and leaving unvoted submissions null; `src/import/recompute-round-results.test.js:209-311` and `src/import/commit-batch.test.js:578-609` verify the outcomes. |
| AC-10 | Batch summary/history exposes batch status, per-file row counts, issue counts, `failureStage`, and `failureSummary` for failed and committed imports | `satisfied` | `src/import/commit-batch.js:87-101,123-130` persists committed and failed batch metadata, `src/import/get-batch-summary.js:65-94` exposes summary fields, and `src/import/list-batches.js:31-44` exposes history fields; `src/import/get-batch-summary.test.js:253-459` and `src/import/list-batches.test.js:173-236` verify failed and committed views. |
| AC-12 | If any canonical write or round-result recompute fails during commit, the batch is not marked `committed` and canonical writes are rolled back | `satisfied` | `src/import/commit-batch.js:20-133` wraps canonical writes plus recompute in one transaction and marks the batch `failed` only after rollback; `src/import/commit-batch.test.js:929-979` verifies recompute failure leaves canonical counts at zero and the batch uncommitted, and `src/import/recompute-round-results.test.js:316-384` verifies the recompute throw rolls back surrounding transactional writes. |

**Invariant Audit** (`preserves:` + repo constitutional)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| INV-01 | SPEC-002 §3 | `preserved` | `src/import/analyze-batch.test.js:96-235` shows analyze/validation updates only staged/audit state while canonical counts remain `[1, 1, 1, 1, 0, 0]`; `src/import/commit-batch.js:20-117` contains canonical writes inside the commit transaction only. |
| INV-02 | SPEC-002 §3 | `preserved` | `src/import/commit-batch.js:20-117` keeps canonical upserts, snapshot reconciliation, recompute, count snapshots, and final `status = "committed"` in one `prisma.$transaction(...)`; `src/import/commit-batch.test.js:929-979` confirms a post-write failure does not leak partial canonical state. |
| INV-04 | SPEC-002 §3 | `preserved` | `src/import/commit-batch.js:82-85` delegates scoring to recompute, and `src/import/recompute-round-results.js:45-129` derives scores/ranks exclusively from canonical `Vote` rows. |
| INV-06 | SPEC-002 §3 | `preserved` | `src/import/commit-batch.js:33-65,304-595` upserts by `sourcePlayerId`, `(leagueSlug, sourceRoundId)`, `normalizedName`, `spotifyUri`, `(roundId, playerId, songId)`, and `(roundId, voterId, songId)` only; no fuzzy or fallback matching paths were introduced. |
| INV-08 | SPEC-002 §3 | `preserved` | `src/import/commit-batch.js:502-552,565-590` writes `sourceImportId: batchId` onto every committed submission and vote; `src/import/commit-batch.test.js:578-637,772-826` verifies persisted provenance on both first and replay commits. |
| INV-09 | SPEC-002 §3 | `preserved` | `src/import/analyze-batch.js:680-700` blocks votes without same-round submissions before commit, and `src/import/recompute-round-results.js:77-85` throws if such a vote still appears transactionally; `src/import/analyze-batch.test.js:316-377` and `src/import/recompute-round-results.test.js:316-384` verify both guardrails. |
| INV-10 | SPEC-002 §3 | `preserved` | `src/import/commit-batch.js:597-707` deletes stale votes, submissions, and rounds only within the committing `gameKey`, while leaving players/artists/songs untouched; `src/import/commit-batch.test.js:645-833` verifies the game-scoped snapshot overwrite behavior. |
| `AGENTS.md` is the canonical repo guidance | `AGENTS.md:35-45` | `preserved` | The audited diff only changes `src/import/commit-batch.js` and `src/import/commit-batch.test.js`; no guidance files were modified. |
| `docs/sdd/` contains the tracked Planner, Implementer, Reviewer, and Orchestrator prompts | `AGENTS.md:35-45` | `preserved` | No `docs/sdd/` prompt files were changed in the audited diff; the work is limited to import implementation/tests. |
| `scripts/sdd/` contains the tracked wrapper and orchestration scripts | `AGENTS.md:35-45` | `preserved` | No `scripts/sdd/` paths were touched in the audited diff. |
| Only the Orchestrator writes `PLAN-*.md` files during execution | `AGENTS.md:35-45` | `preserved` | No `PLAN-*.md` files were added or modified in the audited diff. |
| Do not change active spec contracts or acceptance criteria implicitly in code | `AGENTS.md:35-45` | `preserved` | The implementation stays within the slice contract: transactional commit, replay-safe upserts, game-scoped snapshot overwrite, recompute, and failure-state handling are all implemented directly in `src/import/commit-batch.js:20-133,304-707` without changing spec documents. |
| New dependencies must be explicitly allowed by the active spec or already present in `package.json` when one exists | `AGENTS.md:35-45` | `preserved` | The diff adds no dependency-manifest changes and imports only existing repo/runtime modules plus `@prisma/client`, which the repo already uses (`src/import/commit-batch.js:1-5`; `src/import/commit-batch.test.js:1-14`). |

**Contract Audit** (`contracts:` → §4 items)

| Contract ref | §4 item | Status | Evidence |
|--------------|---------|--------|----------|
| §4d-7 | One transaction boundary covers canonical upserts, game-scoped reconciliation, recompute, count snapshots, and final committed status | `fulfilled` | `src/import/commit-batch.js:20-117` performs all canonical writes, stale-row deletes, `recomputeRoundResults(...)`, count snapshot persistence, and final `status: "committed"`/`committedAt` update inside one transaction. |
| §4d-7 | Precondition errors leave batch status unchanged; post-begin failures roll back writes and mark the batch `failed` with `failureStage = commit` and `failureSummary` | `fulfilled` | `src/import/commit-batch.js:17-21,118-133,142-173` separates precondition errors from post-begin failures, and `src/import/commit-batch.test.js:838-924,929-979` verifies both unchanged-status and failed-after-rollback branches. |
| §4d-7 | Replay safety uses deterministic upsert keys and snapshot overwrite is confined to `batch.gameKey` | `fulfilled` | `src/import/commit-batch.js:304-595` resolves canonical entities by the specified deterministic keys, and `src/import/commit-batch.js:597-707` removes stale rounds/submissions/votes only for the committing game; `src/import/commit-batch.test.js:645-833` verifies replay-safe overwrite semantics. |
| §4d-7 | Canonical field hydration refreshes source-derived fields and writes `sourceImportId` on committed submissions/votes | `fulfilled` | `src/import/commit-batch.js:226-302,421-426,474-476,502-552,565-590` refreshes player/artist/song/round hydrated fields and writes staged submission/vote metadata including `sourceImportId`; `src/import/commit-batch.test.js:512-637` verifies the refreshed canonical state. |
| §4d-8 | Recompute scores/ranks from canonical votes only, use dense ranking, leave unvoted submissions null, and throw on orphan votes | `fulfilled` | `src/import/recompute-round-results.js:7-33,45-129` implements dense ranking from canonical vote sums, null-reset behavior for unvoted submissions, and orphan-vote throws; `src/import/recompute-round-results.test.js:209-384` verifies all four behaviors. |
| §4b-1 | `ImportBatch` status/count/failure fields support `parsed`/`ready`/`committed`/`failed` semantics and summary/history exposure | `fulfilled` | `prisma/schema.prisma:118-140` defines the required `ImportBatch` fields, `src/import/analyze-batch.js:141-156` sets `ready`/`failed` validation semantics, `src/import/commit-batch.js:87-101,123-130` sets committed/commit-failed semantics, and `src/import/get-batch-summary.js:65-94` plus `src/import/list-batches.js:31-44` expose them. |

**Verdict:** `confirmed`

All audited acceptance criteria, invariants, and contracts passed for TASK-08.
Applied instruction: acceptance criteria already satisfied by existing code; diff may not reflect acceptance criteria. if no diff exists for an AC, use existing codebase to validate these ACs:  AC-07

***
