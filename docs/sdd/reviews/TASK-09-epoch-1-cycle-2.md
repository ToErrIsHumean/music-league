***

### Reviewer Verdict — TASK-09

**AC Audit** (`validates:` from §6)

| AC | Criterion (§5 text) | Status | Evidence |
|----|---------------------|--------|----------|
| AC-01 | Ingesting a valid four-file export bundle creates one `ImportBatch`, four `ImportSourceFile` rows, and staged row counts equal the parsed CSV row counts | `satisfied` | `src/import/workflow-integration.test.js:93-186,238-260` verifies parsed row counts, persisted four-row `ImportSourceFile` staging metadata, and a single batch history entry for the clean bundle. |
| AC-02 | Missing any required source file or required header yields a blocking `ImportIssue` and the batch fails validation without canonical-table writes | `satisfied` | `src/import/workflow-integration.test.js:438-521` stages a bundle missing `votes.csv` and a required competitors header, then verifies blocking issues, failed validation, commit rejection, and zero canonical counts. |
| AC-03 | A clean bundle can be staged and analyzed to `ready` with zero blocking issues and zero canonical-table writes before commit | `satisfied` | `src/import/workflow-integration.test.js:188-237` proves the clean batch reaches `ready`, returns zero blocking issues, and leaves all canonical table counts at zero before commit. |
| AC-04 | `listImportBatchIssues(batchId)` returns every blocking issue with row-level context sufficient for debugging | `satisfied` | `src/import/workflow-integration.test.js:449-483,535-580` asserts complete blocking-issue lists for missing-file/header and duplicate-orphan-vote failures, including row previews and source row numbers. |
| AC-05 | Attempting to commit any batch that is not `ready` fails and leaves canonical tables unchanged | `satisfied` | `src/import/workflow-integration.test.js:509-521,595-607` verifies failed-validation batches reject at commit time and leave every canonical table count at zero. |
| AC-06 | Committing a `ready` batch creates or upserts the required canonical players, rounds, artists, songs, submissions, and votes with correct relationships, hydrated source-derived fields, and `sourceImportId` set on committed submissions and votes | `satisfied` | `src/import/workflow-integration.test.js:264-428,621-832` verifies committed entity counts, round hydration on replay, canonical submission/vote relationships, recomputed results, and `sourceImportId` persistence on committed submissions and votes. |
| AC-07 | A batch containing a vote that cannot be attached to a submission in the same round fails validation and cannot commit | `satisfied` | `src/import/test-fixtures/unresolved-duplicate-bundle/submissions.csv:1-2` and `src/import/test-fixtures/unresolved-duplicate-bundle/votes.csv:1-3` create the orphan-vote case, and `src/import/workflow-integration.test.js:531-607` verifies failed validation plus commit rejection. |
| AC-08 | Re-ingesting and committing the same logical bundle, or a later full-snapshot bundle with the same derived `gameKey`, does not create duplicate canonical players, rounds, artists, songs, submissions, or votes and removes stale game-scoped rounds, submissions, and votes omitted from the incoming bundle | `satisfied` | `src/import/workflow-integration.test.js:617-832` commits two snapshots for `game-77` and verifies reused global counts, one remaining game-scoped round, and stale submissions/votes removed from the later snapshot. |
| AC-09 | After commit, every affected round has `Submission.score` and `Submission.rank` values equal to those derived from canonical `Vote` rows, with unvoted submissions left null | `satisfied` | `src/import/workflow-integration.test.js:372-428,743-769` checks score/rank values against canonical votes for the clean and replayed bundles, including a `null`/`null` unvoted submission. |
| AC-10 | Batch summary/history exposes batch status, per-file row counts, issue counts, `failureStage`, and `failureSummary` for failed and committed imports | `satisfied` | `src/import/workflow-integration.test.js:129-157,238-260,333-363,484-507,581-593,904-932,977-989` verifies summary/history status, row counts, issue counts, and failure metadata across ready, failed, committed, and commit-failed states. |
| AC-11 | The internal import workflow is adapter-neutral: a bundle-path ingest followed by issue listing, summary, and commit can be exercised entirely through the §4d service contracts | `satisfied` | `src/import/workflow-integration.test.js:93-114,188-193,265-268,449-451,535-540,621-644,904-911` drives the workflow through `parseMusicLeagueBundle`, `stageImportBundle`, `analyzeImportBatch`, `listImportBatchIssues`, `getImportBatchSummary`, `listImportBatches`, and `commitImportBatch`. |
| AC-12 | If any canonical write or round-result recompute fails during commit, the batch is not marked `committed` and canonical writes are rolled back | `satisfied` | `src/import/workflow-integration.test.js:837-940,945-1002` separately injects canonical-write and recompute failures, then verifies failed batch state plus zero canonical rows after each rollback. |
| AC-13 | A readable bundle containing duplicate source-key rows in any source file yields blocking `duplicate_source_row` issues and does not fail staging as a raw database constraint error | `satisfied` | `src/import/workflow-integration.test.js:531-580` stages a readable bundle with duplicate vote keys and verifies surfaced `duplicate_source_row` diagnostics rather than a staging crash. |
| AC-14 | On normal league-sized datasets, parse+stage and commit each complete within the milestone target budget, and summary reads return quickly enough to feel interactive | `out of scope [Final Pass]` | `docs/specs/0.1.2/SPEC-002-csv-import-pipeline-slice-TASK-09.md:472` marks this criterion `manual`, so it is not contestable in diff-only review. |
| AC-15 | `getImportBatchSummary(batchId)` exposes batch workflow stage progress and whether the batch is still awaiting system work | `satisfied` | `src/import/workflow-integration.test.js:141-150,213-221,334-342,485-493,915-923,978-986` verifies `workflow.stages` and `awaiting` across parsed, ready, failed-validate, committed, and failed-commit summaries. |

**Invariant Audit** (`preserves:` + repo constitutional)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| INV-01 | SPEC-002 §3 | `preserved` | The diff is test-only, and the added suite continues to require zero canonical writes before successful commit via pre-commit count checks, failed-validation commit rejection, and rollback assertions (`src/import/workflow-integration.test.js:189-237,509-521,595-607,904-940,968-1002`). |
| INV-02 | SPEC-002 §3 | `preserved` | The new coverage keeps atomic-commit expectations intact by asserting either full commit success or full rollback only (`src/import/workflow-integration.test.js:264-428,621-832,837-1002`). |
| INV-03 | SPEC-002 §3 | `preserved` | New failure fixtures still require deterministic validation to gate commitability and drive batches to `failed` when blocking issues remain (`src/import/workflow-integration.test.js:460-507,542-590`). |
| INV-04 | SPEC-002 §3 | `preserved` | The integration assertions continue to derive `Submission.score` and `Submission.rank` from committed vote state, including unvoted `null` values (`src/import/workflow-integration.test.js:372-428,743-769`). |
| INV-05 | SPEC-002 §3 | `preserved` | The fixture set models complete four-file bundles for clean and replay scenarios and uses a deliberately missing file only to verify blocking-issue behavior (`src/import/workflow-integration.test.js:93-114,438-445,617-644`). |
| INV-06 | SPEC-002 §3 | `preserved` | Replay coverage continues to rely on deterministic source keys and `gameKey`-scoped matching only (`src/import/test-fixtures/replay-snapshot-one/rounds.csv:1-3`; `src/import/workflow-integration.test.js:617-832`). |
| INV-07 | SPEC-002 §3 | `preserved` | Failure scenarios assert durable issue inspection through issue-list, summary, and history surfaces without any staged-data editing path (`src/import/workflow-integration.test.js:449-507,535-593,904-932,977-989`). |
| INV-08 | SPEC-002 §3 | `preserved` | The committed-state assertions explicitly require `sourceImportId` on submissions and votes in both clean and replayed commits (`src/import/workflow-integration.test.js:372-428,743-793`). |
| INV-09 | SPEC-002 §3 | `preserved` | The unresolved-vote fixture and test continue to require validation failure and commit rejection when a vote cannot attach to a submission in the same round (`src/import/workflow-integration.test.js:531-607`). |
| INV-10 | SPEC-002 §3 | `preserved` | The replay test verifies snapshot-destructive overwrite within one `gameKey` while leaving global entity counts non-destructive (`src/import/workflow-integration.test.js:646-832`). |
| `AGENTS.md` is the canonical repo guidance | `AGENTS.md` | `preserved` | The audited diff adds fixture CSVs and `src/import/workflow-integration.test.js` only; no guidance files were modified. |
| `docs/sdd/` contains the tracked Planner, Implementer, Reviewer, and Orchestrator prompts | `AGENTS.md` | `preserved` | No tracked prompt files were touched; the work is limited to import test fixtures and one integration test file. |
| `scripts/sdd/` contains the tracked wrapper and orchestration scripts | `AGENTS.md` | `preserved` | No `scripts/sdd/` paths appear in the audited diff. |
| Only the Orchestrator writes `PLAN-*.md` files during execution | `AGENTS.md` | `preserved` | No `PLAN-*.md` files were added or modified in the audited diff. |
| Do not change active spec contracts or acceptance criteria implicitly in code | `AGENTS.md` | `preserved` | The diff adds coverage only; it does not modify spec files or production import behavior. |
| New dependencies must be explicitly allowed by the active spec or already present in `package.json` when one exists | `AGENTS.md` | `preserved` | The audited diff does not modify `package.json` or introduce new dependencies. |

**Contract Audit** (`contracts:` → §4 items)

| Contract ref | §4 item | Status | Evidence |
|--------------|---------|--------|----------|
| §4d-1 | `parseMusicLeagueBundle(input)` parses bundle-path fixtures into typed rows, `gameKey`, and surfaced parser issues | `fulfilled` | `src/import/workflow-integration.test.js:93-112,438-445` verifies parsed row counts, derived `gameKey`, and surfaced `missing_header`/`missing_file` issues from fixture bundles. |
| §4d-2 | `stageImportBundle(input)` persists parsed staging state, source files, row counts, parser issues, and duplicate-source-row diagnostics | `fulfilled` | `src/import/workflow-integration.test.js:114-186,531-580` verifies persisted `ImportSourceFile` rows, returned stage row counts, and surfaced `duplicate_source_row` diagnostics for readable bundles. |
| §4d-3 | `analyzeImportBatch(batchId)` classifies batches as `ready` or `failed` with deterministic summary counts and blocking-issue totals | `fulfilled` | `src/import/workflow-integration.test.js:188-221,460-507,542-590,619-638` covers clean `ready` analysis plus multiple `failed` validation outcomes with summary counts. |
| §4d-4 | `listImportBatchIssues(batchId)` returns blocking issues with reconstructable row context | `fulfilled` | `src/import/workflow-integration.test.js:189-190,449-483,535-580` asserts issue codes, blocking flags, row numbers, and row previews across parser and validation failures. |
| §4d-5 | `getImportBatchSummary(batchId)` exposes workflow stages, counts, affected rounds, and failure metadata | `fulfilled` | `src/import/workflow-integration.test.js:129-157,213-229,333-351,484-503,581-590,831-832,914-925,977-988` verifies summary data in parsed, ready, failed, committed, and failed-commit states. |
| §4d-6 | `listImportBatches(input?)` exposes filtered batch history with commit/failure snapshots | `fulfilled` | `src/import/workflow-integration.test.js:189-193,238-260,451-507,537-540,591-593,701-705,796-830,906-932` verifies history rows for ready, failed, and committed batches, including status filters and persisted snapshot fields. |
| §4d-7 | `commitImportBatch(batchId)` commits ready batches transactionally, rejects non-ready batches, and records rollback metadata on commit failure | `fulfilled` | `src/import/workflow-integration.test.js:264-428,509-521,595-607,621-832,837-1002` proves successful commits, replay, non-ready rejection, and both canonical-write and recompute-failure rollback branches. |
| §4d-8 | `recomputeRoundResults(roundIds)` recomputes scores/ranks from committed votes, leaves unvoted submissions null, and causes rollback on recompute failure | `fulfilled` | `src/import/workflow-integration.test.js:372-428,743-769,957-1002` verifies recomputed scores/ranks, `null` results for unvoted submissions, and rollback when recompute throws during commit. |

**Verdict:** `confirmed`

All diff-scope AC, invariant, and contract rows passed for TASK-09; AC-14 remains out of scope [Final Pass].

***
