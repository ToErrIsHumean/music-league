***

### Reviewer Verdict — TASK-09

**AC Audit** (`validates:` from §6)

| AC | Criterion (§5 text) | Status | Evidence |
|----|---------------------|--------|----------|
| AC-01 | Ingesting a valid four-file export bundle creates one `ImportBatch`, four `ImportSourceFile` rows, and staged row counts equal the parsed CSV row counts | `unsatisfied` | `src/import/workflow-integration.test.js:114-127,188-210` proves the staged row counts and single batch history entry, but the diff never inspects `ImportSourceFile` persistence, so the “four ImportSourceFile rows” clause is not validated. |
| AC-02 | Missing any required source file or required header yields a blocking `ImportIssue` and the batch fails validation without canonical-table writes | `satisfied` | `src/import/workflow-integration.test.js:388-471` stages a bundle missing `votes.csv` and with a bad competitors header, then verifies blocking issues, failed validation, commit rejection, and zero canonical counts. |
| AC-03 | A clean bundle can be staged and analyzed to `ready` with zero blocking issues and zero canonical-table writes before commit | `unsatisfied` | `src/import/workflow-integration.test.js:149-187` proves the clean bundle reaches `ready` with zero blocking issues, but the first canonical-count assertion appears only after commit at `src/import/workflow-integration.test.js:314-321`, so “zero canonical-table writes before commit” is not checked. |
| AC-04 | `listImportBatchIssues(batchId)` returns every blocking issue with row-level context sufficient for debugging | `satisfied` | `src/import/workflow-integration.test.js:399-433,485-529` asserts complete blocking-issue lists for missing-file/header and duplicate/unresolved-vote failures, including `rowPreview` context. |
| AC-05 | Attempting to commit any batch that is not `ready` fails and leaves canonical tables unchanged | `satisfied` | `src/import/workflow-integration.test.js:459-471,545-557` verifies failed-validation batches reject at commit time and leave all canonical counts at zero. |
| AC-06 | Committing a `ready` batch creates or upserts the required canonical players, rounds, artists, songs, submissions, and votes with correct relationships, hydrated source-derived fields, and `sourceImportId` set on committed submissions and votes | `satisfied` | `src/import/workflow-integration.test.js:214-377,596-745` verifies committed entity counts, round refresh, submission/vote relationships, recomputed scores, and `sourceImportId` on committed submissions and votes across clean and replayed imports. |
| AC-07 | A batch containing a vote that cannot be attached to a submission in the same round fails validation and cannot commit | `satisfied` | `src/import/test-fixtures/unresolved-duplicate-bundle/submissions.csv:1-2` and `src/import/test-fixtures/unresolved-duplicate-bundle/votes.csv:1-3` create the orphan-vote condition, and `src/import/workflow-integration.test.js:481-548` verifies failed validation plus commit rejection. |
| AC-08 | Re-ingesting and committing the same logical bundle, or a later full-snapshot bundle with the same derived `gameKey`, does not create duplicate canonical players, rounds, artists, songs, submissions, or votes and removes stale game-scoped rounds, submissions, and votes omitted from the incoming bundle | `satisfied` | `src/import/workflow-integration.test.js:562-783` commits two snapshots for `game-77` and verifies reused global counts, one remaining game-scoped round, and stale submissions/votes removed from the later snapshot. |
| AC-09 | After commit, every affected round has `Submission.score` and `Submission.rank` values equal to those derived from canonical `Vote` rows, with unvoted submissions left null | `satisfied` | `src/import/workflow-integration.test.js:322-377,693-719` checks score/rank values against canonical votes for the clean and replayed bundles, including a `null`/`null` unvoted submission. |
| AC-10 | Batch summary/history exposes batch status, per-file row counts, issue counts, `failureStage`, and `failureSummary` for failed and committed imports | `satisfied` | `src/import/workflow-integration.test.js:129-152,400-457,651-656,810-837` verifies summary/history fields across parsed, ready, failed-validate, committed, and failed-commit states. |
| AC-11 | The internal import workflow is adapter-neutral: a bundle-path ingest followed by issue listing, summary, and commit can be exercised entirely through the §4d service contracts | `satisfied` | `src/import/workflow-integration.test.js:93-152,397-400,481-486,572-595` drives the workflow through `parseMusicLeagueBundle`, `stageImportBundle`, `analyzeImportBatch`, `listImportBatchIssues`, `getImportBatchSummary`, `listImportBatches`, and `commitImportBatch`. |
| AC-12 | If any canonical write or round-result recompute fails during commit, the batch is not marked `committed` and canonical writes are rolled back | `unsatisfied` | `src/import/workflow-integration.test.js:787-845` covers rollback after an injected recompute failure, but the diff never induces a canonical-write failure during commit, so the full AC is not validated. |
| AC-13 | A readable bundle containing duplicate source-key rows in any source file yields blocking `duplicate_source_row` issues and does not fail staging as a raw database constraint error | `satisfied` | `src/import/workflow-integration.test.js:476-548` stages a readable bundle with duplicate vote keys and verifies a surfaced `duplicate_source_row` issue plus normal validation/commit failure handling. |
| AC-14 | On normal league-sized datasets, parse+stage and commit each complete within the milestone target budget, and summary reads return quickly enough to feel interactive | `out of scope [Final Pass]` | `docs/specs/0.1.2/SPEC-002-csv-import-pipeline-slice-TASK-09.md:472` marks this criterion `manual`, so it is not contestable in diff-only review. |
| AC-15 | `getImportBatchSummary(batchId)` exposes batch workflow stage progress and whether the batch is still awaiting system work | `satisfied` | `src/import/workflow-integration.test.js:131-147,171-179,435-443,820-828` verifies `workflow.stages` and `awaiting` across parsed, ready, failed-validate, committed, and failed-commit summaries. |

**Invariant Audit** (`preserves:` + repo constitutional)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| INV-01 | SPEC-002 §3 | `preserved` | The diff is test-only, and the added failure-path assertions continue to require zero canonical writes before successful commit (`src/import/workflow-integration.test.js:464-471,550-557,838-845`). |
| INV-02 | SPEC-002 §3 | `preserved` | The added coverage keeps atomic-commit expectations intact by asserting full success or full rollback only (`src/import/workflow-integration.test.js:214-377,658-783,799-845`). |
| INV-03 | SPEC-002 §3 | `preserved` | New failure fixtures still expect validation to gate commitability and drive the batch to `failed` when blocking issues remain (`src/import/workflow-integration.test.js:398-457,492-540`). |
| INV-04 | SPEC-002 §3 | `preserved` | The integration assertions derive `Submission.score` and `Submission.rank` from committed vote state, including unvoted `null` values (`src/import/workflow-integration.test.js:322-377,693-719`). |
| INV-05 | SPEC-002 §3 | `preserved` | The fixture set models full four-file bundles for valid/replay scenarios and uses a deliberately missing file only to verify blocking-issue behavior (`src/import/test-fixtures/clean-bundle/competitors.csv:1-4`, `src/import/workflow-integration.test.js:388-395`). |
| INV-06 | SPEC-002 §3 | `preserved` | The replay fixtures and assertions continue to rely on deterministic source keys and `gameKey`-scoped replay behavior only (`src/import/test-fixtures/replay-snapshot-one/rounds.csv:1-3`, `src/import/workflow-integration.test.js:562-783`). |
| INV-07 | SPEC-002 §3 | `preserved` | Failure scenarios assert durable issue inspection through issue-list, summary, and history surfaces without any staged-data editing path (`src/import/workflow-integration.test.js:399-457,485-544,810-837`). |
| INV-08 | SPEC-002 §3 | `preserved` | The committed-state assertions explicitly require `sourceImportId` on submissions and votes in both clean and replayed commits (`src/import/workflow-integration.test.js:322-377,693-745`). |
| INV-09 | SPEC-002 §3 | `preserved` | The unresolved-vote fixture and test continue to require validation failure and commit rejection when a vote cannot attach to a submission in the same round (`src/import/workflow-integration.test.js:481-548`). |
| INV-10 | SPEC-002 §3 | `preserved` | The replay test verifies snapshot-destructive overwrite within one `gameKey` while leaving global entity counts non-destructive (`src/import/workflow-integration.test.js:671-783`). |
| `AGENTS.md` is the canonical repo guidance | `AGENTS.md` | `preserved` | The audited diff adds fixture CSVs and `src/import/workflow-integration.test.js` only; no guidance files were modified. |
| `docs/sdd/` contains the tracked Planner, Implementer, Reviewer, and Orchestrator prompts | `AGENTS.md` | `preserved` | No tracked prompt files were touched; the work is limited to test fixtures and one integration test file. |
| `scripts/sdd/` contains the tracked wrapper and orchestration scripts | `AGENTS.md` | `preserved` | No `scripts/sdd/` paths appear in the audited diff. |
| Only the Orchestrator writes `PLAN-*.md` files during execution | `AGENTS.md` | `preserved` | No `PLAN-*.md` files were added or modified in the audited diff. |
| Do not change active spec contracts or acceptance criteria implicitly in code | `AGENTS.md` | `preserved` | The diff adds coverage only; it does not modify spec files or production import behavior. |
| New dependencies must be explicitly allowed by the active spec or already present in `package.json` when one exists | `AGENTS.md` | `preserved` | The audited diff does not modify `package.json` or introduce new dependencies. |

**Contract Audit** (`contracts:` → §4 items)

| Contract ref | §4 item | Status | Evidence |
|--------------|---------|--------|----------|
| §4d-1 | `parseMusicLeagueBundle(input)` parses bundle-path fixtures into typed rows, `gameKey`, and surfaced parser issues | `fulfilled` | `src/import/workflow-integration.test.js:93-112,388-395` verifies parsed row counts, derived `gameKey`, and surfaced `missing_header`/`missing_file` issues from fixture bundles. |
| §4d-2 | `stageImportBundle(input)` persists parsed staging state, row counts, parser issues, and duplicate-source-row diagnostics | `unverifiable` | `src/import/workflow-integration.test.js:114-127,481-529` verifies returned row counts and later duplicate issue surfacing, but the diff never inspects `ImportSourceFile` persistence or the stage-failure branch. |
| §4d-3 | `analyzeImportBatch(batchId)` classifies batches as `ready` or `failed` with deterministic summary counts and blocking-issue totals | `fulfilled` | `src/import/workflow-integration.test.js:149-168,398-411,492-543,567-569,586-588` covers clean `ready` analysis plus multiple `failed` validation outcomes with summary counts. |
| §4d-4 | `listImportBatchIssues(batchId)` returns blocking issues with reconstructable row context | `fulfilled` | `src/import/workflow-integration.test.js:150,399-433,485-529` asserts issue codes, blocking flags, row numbers, and row previews across parser and validation failures. |
| §4d-5 | `getImportBatchSummary(batchId)` exposes workflow stages, counts, affected rounds, and failure metadata | `fulfilled` | `src/import/workflow-integration.test.js:129-147,151-187,400-457,655,810-830` verifies summary data in parsed, ready, failed, committed, and failed-commit states. |
| §4d-6 | `listImportBatches(input?)` exposes filtered batch history with commit/failure snapshots | `fulfilled` | `src/import/workflow-integration.test.js:152,188-212,401-457,487-544,651-780,812-837` verifies history rows for ready, failed, and committed batches, including status filters and persisted count snapshots. |
| §4d-7 | `commitImportBatch(batchId)` commits ready batches transactionally, rejects non-ready batches, and records rollback metadata on commit failure | `unverifiable` | `src/import/workflow-integration.test.js:214-377,459-471,545-557,571-595,799-845` proves successful commits, replay, non-ready rejection, and recompute-failure rollback, but does not exercise a canonical-write failure after transactional work begins. |
| §4d-8 | `recomputeRoundResults(roundIds)` recomputes scores/ranks from committed votes, leaves unvoted submissions null, and causes rollback on recompute failure | `fulfilled` | `src/import/workflow-integration.test.js:322-377,693-719,799-845` verifies recomputed scores/ranks, `null` results for unvoted submissions, and rollback when recompute throws during commit. |

**Verdict:** `contested`

- AC-01 is not fully validated because the new suite never checks that staging created four `ImportSourceFile` rows (`src/import/workflow-integration.test.js:114-127,188-210`).
- AC-03 is not fully validated because the clean-path test never inspects canonical tables before commit; canonical counts are asserted only after commit (`src/import/workflow-integration.test.js:149-187,314-321`).
- AC-12 is not fully validated because only the recompute-failure rollback path is exercised; no test covers a canonical-write failure during commit (`src/import/workflow-integration.test.js:787-845`).

***
