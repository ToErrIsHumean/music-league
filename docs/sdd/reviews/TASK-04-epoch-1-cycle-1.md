### Reviewer Verdict — TASK-04

**AC Audit** (`validates:` from §6)

| AC | Criterion (§5 text) | Status | Evidence |
|----|---------------------|--------|----------|
| AC-06 | Landing game cards and game headers use event-derived timeframe labels and omit timeframe when no event dates exist. ORM/import bookkeeping timestamps are not displayed. | `satisfied` | `deriveGameTimeframe` consumes only `Round.occurredAt`, `Submission.submittedAt`, and `Vote.votedAt`, returns `null` with no event dates, and formats only those dates (`src/archive/m8-derivations.js:157`). Landing summaries and game headers consume/suppress that nullable label (`src/archive/route-skeletons.js:159`, `src/archive/route-skeletons.js:635`, `src/archive/route-skeletons.js:1525`, `src/archive/route-skeletons.js:1570`). Unit coverage passed for empty and widened timeframe cases (`src/archive/m8-derivations.test.js:27`). |
| AC-09 | Leaderboard ties render `T<rank>` rows with the total points -> round wins -> display name hierarchy and an accessible footnote whenever ties exist. | `satisfied` | `deriveLeaderboardRows` sorts by total score, round wins, then display name, computes shared tie ranks, and emits a footnote (`src/archive/m8-derivations.js:197`). Game rendering uses `T<rank>` labels and `ariaLabel` for tied rows, then renders the footnote with `role="note"` (`src/archive/route-skeletons.js:1583`, `src/archive/route-skeletons.js:1598`). Unit coverage passed (`src/archive/m8-derivations.test.js:63`). |
| AC-16 | A song shown as `First-time` or `Returning` in `/songs` shows the same verdict on `/songs/[songId]`. | `satisfied` | The catalog row and song detail both call `deriveArchiveSongFamiliarity` over exact-song submissions (`src/archive/m8-derivations.js:586`, `src/archive/route-skeletons.js:1093`). Route-skeleton integration asserts the `/songs` row familiarity equals the detail-route familiarity (`prisma/tests/route-skeletons.test.js:120`). Unit and route-skeleton integration tests passed. |
| AC-24 | Round vote disclosures and player voting-history tables attribute votes only through same-round song matches; recurring songs in other rounds or games never receive those votes. | `satisfied` | `mapVotesToRoundSubmissions` keys attribution by `Vote.roundId + Vote.songId`, initializes groups by submission id, and throws on missing/duplicate same-round matches (`src/archive/m8-derivations.js:418`). The alpha round breakdown, route round disclosure, and player received-vote history now consume that mapping (`src/archive/archive-utils.js:444`, `src/archive/route-skeletons.js:838`, `src/archive/route-skeletons.js:1296`). Unit coverage passed for recurring-song cross-round attribution (`src/archive/m8-derivations.test.js:103`). |

**Invariant Audit** (`preserves:` + repo constitutional)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| INV-03 | spec | `preserved` | Current/completed route data remains derived from `Game.finished` in game summaries and landing buckets (`src/archive/route-skeletons.js:159`, `src/archive/route-skeletons.js:534`). |
| INV-04 | spec | `preserved` | Game timeframe derivation uses only imported event timestamps and returns `null` instead of falling back to bookkeeping timestamps (`src/archive/m8-derivations.js:157`). Landing and game rendering omit missing labels rather than displaying `createdAt` (`src/archive/route-skeletons.js:1525`, `src/archive/route-skeletons.js:1570`). |
| INV-05 | spec | `preserved` | Tie grouping preserves shared ranks and tied rows render `T<rank>` pills with tie-specific aria labels (`src/archive/m8-derivations.js:243`, `src/archive/route-skeletons.js:1583`). |
| INV-06 | spec | `preserved` | The diff centralizes game timeframe, leaderboard, search normalization/catalog, archive song familiarity, song chronology, vote attribution, and round ordering in `src/archive/m8-derivations.js`, and route loaders consume those helpers (`src/archive/m8-derivations.js:793`, `src/archive/route-skeletons.js:1`). |
| INV-11 | spec | `violated` | The song origin labels no longer preserve the player-song-in-round evidence: `deriveSongAppearanceFacts` returns first-appearance rows with `submitterName` (`src/archive/m8-derivations.js:338`), but `buildSongOriginLabels` reads `songOrigin.playerName` and `artistOrigin.playerName` (`src/archive/route-skeletons.js:351`, `src/archive/route-skeletons.js:359`). Rendered origin metadata therefore becomes `undefined in <round>` / `undefined first brought...`, losing the submitter identity for the provenance claim rendered in `SongContent` (`src/archive/route-skeletons.js:1851`). |
| INV-12 | spec | `preserved` | Route ID parsing and round ownership checks run before rendering round detail; rounds under the wrong game return status context instead of detail content (`src/archive/route-skeletons.js:668`, `src/archive/route-skeletons.js:812`). Song/player routes do not fabricate game context (`src/archive/route-skeletons.js:1104`, `src/archive/route-skeletons.js:1375`). |
| INV-14 | spec | `preserved` | Song chronology compares `Submission.submittedAt`, then `Round.occurredAt`, then stable submission id, and route song history uses that helper (`src/archive/m8-derivations.js:300`, `src/archive/route-skeletons.js:385`, `src/archive/route-skeletons.js:1100`). The focused test includes misleading `createdAt` values and still orders by event chronology (`src/archive/m8-derivations.test.js:127`). |
| INV-18 | spec | `preserved` | Vote attribution resolves from `roundId + songId`, stores downstream groups by `submissionId`, and is used for round and player vote displays (`src/archive/m8-derivations.js:423`, `src/archive/archive-utils.js:469`, `src/archive/route-skeletons.js:881`, `src/archive/route-skeletons.js:1305`). |
| INV-19 | spec | `preserved` | Canonical round ordering sorts populated `sequenceNumber`, then populated `occurredAt`, then stable `id`; game route round lists consume `deriveGameRoundListItems` (`src/archive/m8-derivations.js:456`, `src/archive/m8-derivations.js:522`, `src/archive/route-skeletons.js:631`). |
| Repo-AGENTS-canonical | guidance | `preserved` | The diff does not modify `AGENTS.md` or introduce alternate repo guidance. |
| Repo-bolder-utils-defaults | guidance | `preserved` | `package.json` retains the `bolder-*` SDD package bins; the only script change adds the new unit test file to `test:unit` (`package.json:13`, `package.json:22`). |
| Repo-prompt-overrides-location | guidance | `preserved` | The diff does not add repo-local prompt overrides outside `docs/sdd/`. |
| Repo-sdd-runtime-artifacts | guidance | `preserved` | Review/diff artifacts remain under `docs/sdd/`; no runtime artifact path is moved by the implementation diff. |
| Repo-plan-ownership | guidance | `preserved` | The diff does not modify any `PLAN-*.md` file. |
| Repo-spec-contract-stability | guidance | `preserved` | The diff does not edit active spec contracts or acceptance criteria. |
| Repo-dependency-policy | guidance | `preserved` | No dependency is added; `package.json` dependencies remain unchanged (`package.json:30`). |

**Contract Audit** (`contracts:` → §4 items)

| Contract ref | §4 item | Status | Evidence |
|--------------|---------|--------|----------|
| §4b-1 | Schema changes | `fulfilled` | No Prisma schema or migration change appears in the diff; the implementation reads existing game, round, submission, and vote fields only (`src/archive/m8-derivations.js:657`, `src/archive/route-skeletons.js:575`). |
| §4d-1 | Route href builders | `fulfilled` | Shared helpers import and emit the existing stable route builders for game, round, song, player, and song-search hrefs (`src/archive/m8-derivations.js:3`, `src/archive/route-utils.js:70`). |
| §4d-2 | Game timeframe derivation | `fulfilled` | `deriveGameTimeframe` returns `null` for no usable event dates, computes start/end from event timestamps, emits range labels when dates differ, and marks widened sources (`src/archive/m8-derivations.js:157`). |
| §4d-3 | Leaderboard and tie derivation | `fulfilled` | `deriveLeaderboardRows` aggregates scored submissions by player, excludes averages, sorts by total points/round wins/display name, marks ties, and returns a footnote (`src/archive/m8-derivations.js:197`). |
| §4d-4 | Search normalization and catalog query | `fulfilled` | `normalizeArchiveSearch` handles arrays, punctuation-only input, and empty output; `getSongCatalog` normalizes query/filter/sort, matches normalized title or artist, caps empty-query results at 100, and returns familiarity/appearance/best-finish facts; header suggestions suppress empty queries, cap at eight, and omit player matches (`src/archive/m8-derivations.js:79`, `src/archive/m8-derivations.js:645`, `src/archive/m8-derivations.js:739`). |
| §4d-5 | Song familiarity verdict | `fulfilled` | `deriveArchiveSongFamiliarity` uses exact-song submission count to return mutually exclusive `first-time` or `returning` verdicts with archive appearance count (`src/archive/m8-derivations.js:279`). |
| §4d-6 | Page data loaders | `fulfilled` | TASK-04's owned shared pieces are present: Prisma injection/client ownership convention plus reusable `ready`, `not-found`, `sparse`, and `StatusNoticeModel` constructors (`src/archive/m8-derivations.js:30`, `src/archive/m8-derivations.js:44`, `src/archive/m8-derivations.js:66`). Route-specific loader completion remains explicitly delegated to later tasks by the slice. |
| §4d-9 | Route params, metadata, and retired-param guard | `fulfilled` | Positive ID parsing, retired overlay param stripping, and metadata fallbacks are present and covered by route-utils tests (`src/archive/route-utils.js:10`, `src/archive/route-utils.js:60`, `src/archive/route-utils.js:130`, `src/archive/route-utils.test.js:14`). |
| §4d-15 | Song appearance chronology | `broken` | The chronology helper itself orders by submitted date, round date fallback, and stable id (`src/archive/m8-derivations.js:300`), but its first-appearance row shape is misconsumed by the changed origin-label integration: helper rows expose `submitterName` (`src/archive/m8-derivations.js:350`), while `buildSongOriginLabels` reads nonexistent `playerName` fields (`src/archive/route-skeletons.js:351`, `src/archive/route-skeletons.js:359`). This breaks the song-origin provenance produced from the §4d-15 facts. |
| §4d-16 | Vote-to-submission attribution | `fulfilled` | `mapVotesToRoundSubmissions` resolves votes by same-round song key, rejects missing/duplicate same-round targets, returns maps keyed by submission/vote id, and consumers group display by submission id (`src/archive/m8-derivations.js:418`, `src/archive/archive-utils.js:444`, `src/archive/route-skeletons.js:838`, `src/archive/route-skeletons.js:1296`). |
| §4d-17 | Game round ordering and round-list derivation | `fulfilled` | `compareGameRoundAscending` and `deriveGameRoundListItems` implement sequence/date/id ordering, derive hrefs, submission counts, scoring status, winners, summaries, and playlist URLs without memory-board dependence (`src/archive/m8-derivations.js:456`, `src/archive/m8-derivations.js:522`). |

**Verdict:** `contested`

Failing items:
- INV-11: song origin provenance renders with missing submitter identity because the new first-appearance row exposes `submitterName` while the route still reads `playerName` (`src/archive/m8-derivations.js:350`, `src/archive/route-skeletons.js:351`).
- §4d-15: the changed origin-label integration misconsumes the song appearance fact shape, so first-appearance provenance derived from the chronology helper is broken (`src/archive/route-skeletons.js:351`, `src/archive/route-skeletons.js:359`).
