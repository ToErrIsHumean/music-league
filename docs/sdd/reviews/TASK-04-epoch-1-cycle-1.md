### Reviewer Verdict — TASK-04

**AC Audit** (`validates:` from §6)

| AC | Criterion (§5 text) | Status | Evidence |
|----|---------------------|--------|----------|
| AC-04 | Any new in-app song tap from round detail or player history opens canonical archive-wide song detail rather than the old current-round row shell or M4 player-scoped song subview | `satisfied` | Round-detail song links build `round + song` hrefs and render canonical modal content in `src/archive/game-archive-page.js:426-430` and `src/archive/game-archive-page.js:649-683`; player notable/history song links now use `roundId` plus `songId` without `playerSubmission` in `src/archive/game-archive-page.js:771-774` and `src/archive/game-archive-page.js:823-827`. Regression tests assert canonical round/detail and player-history links in `prisma/tests/archive-page.test.js:146-203` and `prisma/tests/archive-page.test.js:375-441`. |
| AC-05 | On open, the modal shows song title, artist, and a concise familiarity verdict above the fold, plus known first submitter, most recent submitter, exact-song submission count, artist footprint, and best exact-song finish when those facts exist | `satisfied` | Header renders song title and artist in `src/archive/game-archive-page.js:734-743`; above-fold summary renders familiarity label/copy, first/most recent submitters, exact history, artist footprint, best finish, optional recall comment, and shortcuts in `src/archive/game-archive-page.js:265-336` and `src/archive/game-archive-page.js:649-683`. Render coverage asserts the visible title, artist, verdict, and summary labels in `prisma/tests/archive-page.test.js:190-200`. |
| AC-06 | Submission history renders as provenance evidence, newest first within game groups, preserving current/origin game foregrounding and broader archive grouping when the song spans games | `satisfied` | `getSongMemoryModal()` sorts exact-song history newest-first and groups by game with origin game first in `src/archive/archive-utils.js:1306-1339`; group construction preserves origin-game priority in `src/archive/archive-utils.js:235-256`. Query coverage asserts origin game first, broader archive group retention, and newest row order in `prisma/tests/queries.test.js:1111-1128`. |
| AC-07 | Each history row identifies submitter, round, and result; rank is primary and score is supporting context when available, while missing rank or score still leaves an intelligible row | `satisfied` | History rows carry submitter, round, rank, and score in `src/archive/archive-utils.js:214-232`; rendering links submitter/round and shows rank before score in `src/archive/game-archive-page.js:534-603`. Sparse coverage verifies null rank and score still produce a complete row in `prisma/tests/queries.test.js:1178-1232`. |
| AC-08 | First appearance and most recent appearance render as direct evidence shortcuts when they are distinct submission rows and jump to the corresponding history evidence | `satisfied` | Shortcut derivation emits first and most-recent anchors only when the submission ids differ in `src/archive/archive-utils.js:259-283`; UI anchors jump to `#song-history-submission-<id>` and rows define matching ids in `src/archive/game-archive-page.js:512-531` and `src/archive/game-archive-page.js:541-548`. Tests assert the shortcut payload in `prisma/tests/queries.test.js:1099-1110` and rendered anchor presence in `prisma/tests/archive-page.test.js:195-200`. |
| AC-09 | Player links from history evidence open the submitting player in the evidence row's round context, and round links land on the referenced round URL state | `satisfied` | History row rendering builds player hrefs with `roundId: row.roundId, playerId: row.submitter.id` and round hrefs with `roundId: row.roundId` in `src/archive/game-archive-page.js:534-539`. Render tests assert both evidence-row href forms in `prisma/tests/archive-page.test.js:185-189`. |
| AC-10 | Sparse cases render meaningful detail states: one known submission, no prior artist history, missing rank or score, absent comments, and unresolved or stale origin-song URLs remain contained and intelligible | `satisfied` | Sparse modal coverage verifies a single debut submission with null result/comment, no artist footprint, no shortcuts, and an exact origin history row in `prisma/tests/queries.test.js:1178-1232`; unavailable origin-song handling returns contained state from `src/archive/archive-utils.js:1275-1281` and renders non-alarming copy plus close action in `src/archive/game-archive-page.js:628-646`. Stale URL render coverage is in `prisma/tests/archive-page.test.js:251-282`. |
| AC-11 | M5 ships without global search, fuzzy matching, external metadata enrichment, recommendations, charts, vote-by-vote explainers, or multiple simultaneous familiarity badges | `satisfied` | The audited diff is limited to modal styling, archive-page wiring/rendering, and tests; no search UI, external metadata, recommendation, chart, vote breakdown, or additional familiarity badge surface is introduced in `/home/zacha/music-league-worktrees/M5-task-04/docs/sdd/last-diff-task-04.md:1-958`. Round rows still render one familiarity cue per submission in `src/archive/game-archive-page.js:462-470`. |
| AC-12 | Nested route-state resolution preserves legacy `playerSubmission` direct URLs while new `round + song` links open exactly one canonical song detail surface, including mixed-query URLs | `satisfied` | Route resolution gives `player + playerSubmission` precedence, then canonical song, then player summary in `src/archive/game-archive-page.js:62-121`. Tests cover legacy playerSubmission precedence with simultaneous `song`, mixed player/song canonical behavior, and canonical link absence of `playerSubmission` in `prisma/tests/archive-page.test.js:285-339`, `prisma/tests/archive-page.test.js:342-372`, and `prisma/tests/archive-page.test.js:375-441`. |

**Invariant Audit** (`preserves:` + repo constitutional)

| Invariant | Source | Status | Evidence |
|-----------|--------|--------|----------|
| INV-01 | spec | `preserved` | Canonical song route state resolves through `getSongMemoryModal()` by canonical `songId`, with close/origin behavior separated from the opened song identity in `src/archive/game-archive-page.js:92-108` and `src/archive/archive-utils.js:1313-1340`. |
| INV-04 | spec | `preserved` | The first visible modal header and summary include title, artist, familiarity label, verdict copy, and people/history facts before the history section in `src/archive/game-archive-page.js:649-690` and `src/archive/game-archive-page.js:734-759`. |
| INV-05 | spec | `preserved` | Exact-song history is rendered as individual submission rows keyed by `submissionId`; cross-game/cross-round rows are not collapsed in `src/archive/archive-utils.js:214-256` and `src/archive/game-archive-page.js:541-548`. |
| INV-06 | spec | `preserved` | History grouping marks and orders the origin game first while retaining broader groups in `src/archive/archive-utils.js:235-256`; render output displays the origin game/row pills in `src/archive/game-archive-page.js:582-584` and `src/archive/game-archive-page.js:616-618`. |
| INV-07 | spec | `preserved` | Song-history player and round links point to provenance URL states, not alternate song subviews, in `src/archive/game-archive-page.js:534-539` and `src/archive/game-archive-page.js:560-575`. |
| INV-08 | spec | `preserved` | The diff adds contextual song-memory modal wiring only; no global search, fuzzy matching, instant results, or standalone search UI appears in `/home/zacha/music-league-worktrees/M5-task-04/docs/sdd/last-diff-task-04.md:1-958`. |
| INV-09 | spec | `preserved` | Sparse modal tests prove null rank, null score, and null comment keep identity, verdict, provenance, and history structure intact in `prisma/tests/queries.test.js:1178-1232`. |
| INV-10 | spec | `preserved` | `getSongMemoryModal()` separates exact-song submissions from same-artist submissions, uses artist submissions only for familiarity/artist footprint, and builds history groups from exact-song records in `src/archive/archive-utils.js:1300-1339`. |
| INV-11 | spec | `preserved` | Representative origin selection sorts same-round submissions by `createdAt` ascending with id tiebreaker in `src/archive/archive-utils.js:77-88` and `src/archive/archive-utils.js:1284`; duplicate-origin parity is covered in `prisma/tests/queries.test.js:1138-1175`. |
| INV-12 | spec | `preserved` | Familiarity derivation receives origin-round context in `src/archive/archive-utils.js:1320-1327`, and the shared helper excludes same-round co-occurrence from prior familiarity in `src/archive/song-memory.js:144-168`. |
| `AGENTS.md` is canonical repo guidance | guidance | `preserved` | The implementation diff does not modify `AGENTS.md` or `CLAUDE.md`; changed files are confined to `app/globals.css`, `prisma/tests/archive-page.test.js`, and `src/archive/game-archive-page.js` in `/home/zacha/music-league-worktrees/M5-task-04/docs/sdd/last-diff-task-04.md:1-958`. |
| `bolder-utils` provides SDD role wrappers | guidance | `preserved` | No package metadata or SDD wrapper files are changed in the audited diff. |
| Repo-local prompt overrides live under `docs/sdd/` only when configured | guidance | `preserved` | No prompt override files are added or edited in the audited diff. |
| `docs/sdd/` remains the runtime artifact home | guidance | `preserved` | The audited implementation diff does not relocate runtime artifacts; this verdict is written under `docs/sdd/reviews/` as required. |
| Only the Orchestrator writes `PLAN-*.md` files during execution | guidance | `preserved` | No `PLAN-*.md` files appear in the audited diff. |
| Active spec contracts and acceptance criteria are not implicitly changed in code | guidance | `preserved` | The diff does not edit `docs/specs/`; it only wires/render/tests the approved slice behavior. |
| New dependencies must be explicitly allowed or already present | guidance | `preserved` | No dependency or package-lock changes appear in the audited diff. |

**Contract Audit** (`contracts:` → §4 items)

| Contract ref | §4 item | Status | Evidence |
|--------------|---------|--------|----------|
| §4a-1 | Archive route with canonical song memory state | `fulfilled` | Route resolution ignores nested state without a valid round, resolves canonical song state through `getSongMemoryModal()`, renders unavailable song state, closes to the origin round, and uses evidence-row player/round links in `src/archive/game-archive-page.js:50-121`, `src/archive/game-archive-page.js:628-646`, `src/archive/game-archive-page.js:705-759`, and `src/archive/game-archive-page.js:534-539`. |
| §4c-2 | `ArchiveSongModal` | `fulfilled` | The modal renders header identity, familiarity verdict copy, first/most recent/exact history/artist footprint/best finish facts, optional recall comment, evidence shortcuts, grouped history, origin pills, submitter/round links, and rank-first result copy in `src/archive/game-archive-page.js:265-336` and `src/archive/game-archive-page.js:512-690`. |
| §4c-3 | Contained unavailable song state | `fulfilled` | Unavailable payloads are returned with `originRoundId`, `requestedSongId`, and `closeHref` in `src/archive/archive-utils.js:1275-1281`; the nested shell renders contained copy and a return-to-round action in `src/archive/game-archive-page.js:628-646` and `src/archive/game-archive-page.js:734-759`. |
| §4d-3 | `getSongMemoryModal()` | `fulfilled` | The loader returns `null` for invalid origin rounds, unavailable for unusable origin-song context, selects deterministic origin submissions, hydrates exact and same-artist evidence, derives shared familiarity with origin context, builds exact-song summaries/shortcuts/history groups, and closes to the origin round in `src/archive/archive-utils.js:1233-1341`. Query tests cover archive-wide history, artist footprint, shortcuts, grouping, duplicate origins, sparse state, and unavailable state in `prisma/tests/queries.test.js:1037-1244`. |
| §4d-5 | `buildArchiveHref()` canonical song links | `fulfilled` | New round-detail and player-history song calls pass `roundId + songId` and omit `playerSubmission` in `src/archive/game-archive-page.js:426-430`, `src/archive/game-archive-page.js:771-774`, and `src/archive/game-archive-page.js:823-827`; the helper preserves player precedence for backward compatibility in `src/archive/archive-utils.js:1553-1578`. |
| §4d-7 | Nested route-state resolution | `fulfilled` | `resolveNestedSelection()` considers nested state only after an open round, preserves legacy `playerSubmission` precedence, resolves canonical song next, and falls back to player summary last in `src/archive/game-archive-page.js:50-121`; tests cover legacy, stale, and mixed-query paths in `prisma/tests/archive-page.test.js:285-372`. |
| §4d-8 | Song-memory implementation locality | `fulfilled` | Shared familiarity and ordering live in `src/archive/song-memory.js:1-290`, are consumed by archive utilities in `src/archive/archive-utils.js:1-8`, `src/archive/archive-utils.js:495-512`, and `src/archive/archive-utils.js:1306-1327`, and the UI consumes the resulting modal payload rather than reimplementing classification or history ordering in `src/archive/game-archive-page.js:649-690`. |

**Verdict:** `confirmed`

All audited ACs, invariants, and contracts passed. Verification commands run: `npm test -- --test-reporter=spec` and `node --test prisma/tests/archive-page.test.js prisma/tests/queries.test.js`.
